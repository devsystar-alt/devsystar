# نظام إدارة الأجهزة العسكرية 🛡️

## نظرة عامة

نظام شامل لإدارة الأجهزة العسكرية مطور بـ PHP و MySQL مع واجهة عربية كاملة تدعم الكتابة من اليمين إلى اليسار (RTL). النظام يوفر إدارة متقدمة للمستخدمين والصلاحيات مع تتبع شامل للأنشطة.

## المميزات الرئيسية ✨

### إدارة الأجهزة
- إضافة وتعديل وحذف الأجهزة العسكرية
- تتبع حالة الأجهزة (جيد، معطل، صيانة، مفقود)
- البحث والفلترة المتقدمة
- إدارة الملحقات والقطع الإضافية
- تتبع تواريخ الاستلام والصيانة

### إدارة المستخدمين والصلاحيات
- نظام مستخدمين متعدد المستويات
- صلاحيات مرنة قابلة للتخصيص
- المصادقة الآمنة مع تشفير SHA-256
- تتبع جلسات المستخدمين

### الأمان والحماية
- تشفير كلمات المرور باستخدام SHA-256
- حماية من هجمات SQL Injection باستخدام PDO
- تتبع شامل لجميع الأنشطة
- إدارة جلسات آمنة مع انتهاء صلاحية

### التقارير والتصدير
- تصدير البيانات إلى Excel
- تقارير إحصائية شاملة
- نسخ احتياطية لقاعدة البيانات
- سجل مفصل للأنشطة

## متطلبات التشغيل 📋

### خادم الويب
- PHP 7.4 أو أحدث
- MySQL 5.7 أو أحدث / MariaDB 10.2 أو أحدث
- Apache أو Nginx
- دعم UTF-8

### امتدادات PHP المطلوبة
- PDO MySQL
- mbstring
- fileinfo
- zip (للتصدير المضغوط)

## التثبيت 🚀

### 1. رفع الملفات
```bash
# رفع جميع ملفات النظام إلى مجلد الموقع
# عادة: public_html أو htdocs
```

### 2. إعداد قاعدة البيانات
```sql
-- إنشاء قاعدة بيانات جديدة
CREATE DATABASE military_devices CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- إنشاء مستخدم جديد (اختياري)
CREATE USER 'military_user'@'localhost' IDENTIFIED BY 'secure_password';
GRANT ALL PRIVILEGES ON military_devices.* TO 'military_user'@'localhost';
FLUSH PRIVILEGES;
```

### 3. تعديل إعدادات قاعدة البيانات
قم بتعديل الملف `config.php`:

```php
// إعدادات قاعدة البيانات
define('DB_HOST', 'localhost');
define('DB_NAME', 'military_devices');
define('DB_USER', 'military_user');
define('DB_PASS', 'secure_password');
```

### 4. تشغيل التثبيت
1. اذهب إلى `http://yoursite.com/install.php`
2. تابع تعليمات التثبيت
3. سيتم إنشاء الجداول والمستخدم الافتراضي

### 5. تسجيل الدخول الأول
- **اسم المستخدم:** admin
- **كلمة المرور:** admin123

⚠️ **مهم:** قم بتغيير كلمة مرور المدير فوراً بعد التثبيت

## هيكل الملفات 📁

```
/
├── index.php              # الصفحة الرئيسية (إعادة توجيه)
├── config.php            # إعدادات النظام
├── database.php          # فئة قاعدة البيانات
├── auth.php              # نظام المصادقة والصلاحيات
├── login.php             # صفحة تسجيل الدخول
├── logout.php            # تسجيل الخروج
├── dashboard.php         # لوحة التحكم الرئيسية
├── devices.php           # إدارة الأجهزة
├── add_device.php        # إضافة جهاز جديد
├── edit_device.php       # تعديل جهاز
├── users.php             # إدارة المستخدمين
├── permissions.php       # إدارة الصلاحيات
├── activity_log.php      # سجل الأنشطة
├── export.php            # تصدير البيانات
├── access_denied.php     # صفحة عدم وجود صلاحية
├── install.php           # معالج التثبيت
└── README.md             # هذا الملف
```

## الصلاحيات المتاحة 🔐

| الصلاحية | الوصف |
|----------|--------|
| `عرض_الأجهزة` | عرض قائمة الأجهزة والتفاصيل |
| `إدخال_الأجهزة` | إضافة أجهزة جديدة |
| `تعديل_الأجهزة` | تعديل بيانات الأجهزة |
| `حذف_الأجهزة` | حذف الأجهزة من النظام |
| `صيانة_الأجهزة` | تحديث حالة الصيانة |
| `إدارة_المستخدمين` | إضافة وتعديل المستخدمين |
| `إدارة_الصلاحيات` | تخصيص الصلاحيات |
| `عرض_سجل_الأنشطة` | عرض سجل الأنشطة |
| `تصدير_البيانات` | تصدير البيانات |
| `النسخ_الاحتياطي` | إدارة النسخ الاحتياطية |

## الأمان 🔒

### حماية الملفات الحساسة
أضف الملف `.htaccess` لحماية ملفات الإعدادات:

```apache
# حماية ملفات الإعدادات
<Files "config.php">
    Order Allow,Deny
    Deny from all
</Files>

<Files "database.php">
    Order Allow,Deny
    Deny from all
</Files>

# منع الوصول للملفات الاحتياطية
<Files "*.sql">
    Order Allow,Deny
    Deny from all
</Files>
```

### توصيات الأمان
1. **غير كلمة مرور المدير** فوراً بعد التثبيت
2. **استخدم اتصال HTTPS** في البيئة الإنتاجية
3. **فعّل نسخ احتياطية دورية** لقاعدة البيانات
4. **حدّث النظام** بانتظام
5. **راقب سجل الأنشطة** للكشف عن أي أنشطة مشبوهة

## النشر على الخوادم المجانية 🌐

### InfinityFree
1. رفع الملفات إلى مجلد `htdocs`
2. إنشاء قاعدة بيانات من لوحة التحكم
3. تعديل إعدادات `config.php`
4. تشغيل `install.php`

### 000webhost
1. رفع الملفات إلى `public_html`
2. إنشاء قاعدة MySQL
3. تحديث بيانات الاتصال
4. تشغيل معالج التثبيت

### GitHub Pages (للكود فقط)
النظام يتطلب PHP و MySQL، لذا GitHub Pages غير مناسب للتشغيل المباشر، لكن يمكن استخدامه لحفظ الكود.

## التخصيص 🎨

### تغيير الألوان والتصميم
قم بتعديل ملفات CSS في قسم `<style>` في كل صفحة.

### إضافة حقول جديدة للأجهزة
1. عدّل جدول `الأجهزة` في `database.php`
2. أضف الحقول في نماذج الإدخال والعرض
3. حدّث استعلامات قاعدة البيانات

### إضافة صلاحيات جديدة
1. أضف الصلاحية في دالة `createDefaultPermissions()` في `database.php`
2. استخدم `hasPermission('اسم_الصلاحية')` للتحكم بالوصول

## الدعم والصيانة 🛠️

### ملفات السجلات
- أخطاء PHP: تحقق من ملفات error.log في الخادم
- سجل الأنشطة: متوفر في قاعدة البيانات (جدول سجل_الأنشطة)

### النسخ الاحتياطية
```sql
-- تصدير قاعدة البيانات
mysqldump -u username -p military_devices > backup_$(date +%Y%m%d).sql

-- استعادة النسخة الاحتياطية
mysql -u username -p military_devices < backup_20240101.sql
```

### تحديث النظام
1. أخذ نسخة احتياطية من قاعدة البيانات
2. نسخ الملفات الجديدة
3. تشغيل سكربت التحديث (إن وجد)
4. اختبار النظام

## المطور 👨‍💻

**تصميم وبرمجة: أبو ربيع الحميدي**

- 📞 **الهاتف:** +966 123 456 789
- 📧 **البريد الإلكتروني:** abu.rabee.alhumaydi@email.com

---

## الترخيص 📄

هذا النظام مطور خصيصاً للاستخدامات العسكرية والأمنية. جميع الحقوق محفوظة.

### إخلاء المسؤولية
هذا النظام مخصص للاستخدام الداخلي في البيئات المؤسسية. المطور غير مسؤول عن أي استخدام غير مصرح به أو أضرار قد تنتج عن سوء الاستخدام.

---

## سجل التحديثات 📝

### الإصدار 1.0.0 (2024)
- إصدار أولي كامل
- نظام إدارة الأجهزة العسكرية
- نظام المستخدمين والصلاحيات
- واجهة عربية كاملة مع دعم RTL
- تصدير البيانات وسجل الأنشطة
- نظام الأمان المتقدم

---

*للحصول على الدعم الفني أو الاستفسارات، يرجى التواصل مع المطور*