# دليل النشر - نظام إدارة الأجهزة العسكرية

## 🚀 النشر على GitHub

### الخطوات:
1. **إنشاء مستودع جديد على GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit: Military devices management system"
   git branch -M main
   git remote add origin https://github.com/username/military-devices-system.git
   git push -u origin main
   ```

2. **تحديث الملفات الحساسة**
   - تأكد من عدم رفع كلمات المرور الحقيقية
   - استخدم `.gitignore` لحماية ملفات الإعدادات الحساسة

### ملف `.gitignore` المقترح:
```
# Config files with sensitive data
config_local.php
*.log

# Backup files
*.sql
backups/

# Temp files
tmp/
temp/
```

---

## 🌐 النشر على InfinityFree

### المتطلبات:
- حساب مجاني على [InfinityFree](https://infinityfree.net/)
- قاعدة بيانات MySQL
- دعم PHP 7.4+

### خطوات النشر:

#### 1. رفع الملفات
```bash
# رفع جميع ملفات PHP إلى مجلد htdocs
- index.php
- config.php
- database.php
- auth.php
- login.php
- logout.php
- dashboard.php
- devices.php
- navbar.php
- access_denied.php
- install.php
- .htaccess
- README.md
```

#### 2. إعداد قاعدة البيانات
1. اذهب إلى لوحة تحكم InfinityFree
2. اختر "MySQL Databases"
3. أنشئ قاعدة بيانات جديدة
4. احفظ بيانات الاتصال:
   - Host: (مثل sql123.infinityfree.com)
   - Database Name: (مثل if0_12345_military)
   - Username: (مثل if0_12345)
   - Password: (كلمة المرور التي أنشأتها)

#### 3. تحديث إعدادات قاعدة البيانات
قم بتعديل ملف `config.php`:

```php
// إعدادات قاعدة البيانات لـ InfinityFree
define('DB_HOST', 'sql123.infinityfree.com');
define('DB_NAME', 'if0_12345_military');
define('DB_USER', 'if0_12345');
define('DB_PASS', 'your_database_password');
```

#### 4. تشغيل معالج التثبيت
1. اذهب إلى `http://yoursite.infinityfree.net/install.php`
2. اتبع تعليمات التثبيت
3. سيتم إنشاء الجداول والمستخدم الافتراضي

#### 5. تسجيل الدخول الأول
- **الرابط:** `http://yoursite.infinityfree.net/login.php`
- **اسم المستخدم:** admin
- **كلمة المرور:** admin123

⚠️ **هام:** غيّر كلمة مرور المدير فوراً!

---

## 🌐 النشر على 000webhost

### المتطلبات:
- حساب مجاني على [000webhost](https://000webhost.com/)

### الخطوات:
1. **رفع الملفات إلى `public_html`**
2. **إنشاء قاعدة بيانات MySQL من لوحة التحكم**
3. **تحديث `config.php` ببيانات قاعدة البيانات الجديدة**
4. **تشغيل `install.php`**

---

## 🔧 إعدادات إضافية للإنتاج

### 1. تحسين الأمان
```php
// في config.php
define('PRODUCTION_MODE', true);
define('ENABLE_DEBUG', false);

// تشفير أقوى لكلمات المرور
define('PASSWORD_SALT', 'unique_random_string_for_your_site');
```

### 2. تحسين الأداء
```apache
# في .htaccess
# تمكين ضغط الملفات
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/css text/javascript
</IfModule>

# تخزين مؤقت
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
</IfModule>
```

### 3. النسخ الاحتياطية
```bash
# نسخة احتياطية يومية لقاعدة البيانات
mysqldump -h host -u username -p database_name > backup_$(date +%Y%m%d).sql
```

---

## 📧 دعم ما بعد النشر

### المشاكل الشائعة:

#### خطأ 500 Internal Server Error
- تحقق من صلاحيات الملفات (644 للملفات، 755 للمجلدات)
- تحقق من صحة ملف `.htaccess`
- راجع ملفات الأخطاء في لوحة التحكم

#### خطأ في الاتصال بقاعدة البيانات
- تأكد من صحة بيانات الاتصال في `config.php`
- تحقق من تفعيل امتداد PDO MySQL
- تأكد من إضافة نطاق موقعك إلى قائمة الوصول المسموح

#### مشاكل الترميز العربي
- تأكد من تعيين UTF-8 في قاعدة البيانات
- تحقق من وجود `<meta charset="UTF-8">` في كل صفحة

### التواصل للدعم الفني:
**أبو ربيع الحميدي**
- 📞 **الهاتف:** +966 123 456 789
- 📧 **البريد الإلكتروني:** abu.rabee.alhumaydi@email.com

---

## 🔄 تحديثات مستقبلية

لتحديث النظام:
1. أخذ نسخة احتياطية من قاعدة البيانات
2. رفع الملفات الجديدة
3. تشغيل سكربت التحديث (إن وجد)
4. اختبار النظام

---

*جميع الحقوق محفوظة - نظام إدارة الأجهزة العسكرية 2024*